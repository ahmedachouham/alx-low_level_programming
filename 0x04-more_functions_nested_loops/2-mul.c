include "main.h"
/**
 * mul - checks for checks for a digit (0 through 9).
 * @a: a - Variable
 * @b: b - Variable
 * Return: Always 0.
 */
int main(void)
{
	int mult;

	mult = a * b;

	return (mult);
}
